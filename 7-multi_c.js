 const arg = process.argv[2];
const multi = Number(arg)
  if (!Number.isInteger(multi)) {
console.log("Missing number of occurences");
  } else {
    for (i=0; i<multi; i++) {
    console.log(`C is fun`);
  }
  }


